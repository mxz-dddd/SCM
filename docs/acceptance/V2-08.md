# V2-08 真实运行闭环验收

## 脚本输出

- 被测 commit：`07ee022f96e3682d74b18add5a2c553ce26c35cd`
- 运行命令：`pnpm test:v2-e2e`
- 数据库：独立 `scm_v2_e2e`，从零部署 82 个迁移并执行 seed；Redis 使用隔离 DB 15
- 运行时：真实 NestJS API、真实多租户 Worker、Vite Web、Chromium
- Playwright：5/5 测试通过
- 业务场景：11/11 通过
- P95：未测；V2-08 是运行闭环正确性套件，不复制 P5 性能数据

脚本只通过 Prisma 准备主数据、注入故障和读取最终断言；订单、出库、运输、投递和重放结果均由真实 HTTP 命令及 Worker 产生。所有轮询都有超时和最后状态诊断，未使用固定长 sleep。

## 场景结果

| 场景标识 | 结果 | 验证点 |
| --- | --- | --- |
| `oms-release-auto-wms-tms` | 通过 | OMS release 后 Worker 自动创建唯一 WMS Outbound 与 TMS TransportOrder |
| `event-replay-idempotent` | 通过 | 原事件重放不重复创建下游对象 |
| `business-number-unified` | 通过 | OMS、WMS、TMS 业务号使用统一规则且租户范围唯一 |
| `consumer-temporary-failure-retry` | 通过 | 临时失败增加 attempt 并在退避后成功 |
| `dead-letter-replay-partition-block` | 通过 | 必需消费者死信阻塞同分区，人工重放后恢复 |
| `delivery-order-every-event` | 通过 | 同分区保持源顺序，相同 aggregateVersion 的不同 eventId 均处理 |
| `multi-tenant-worker` | 通过 | 同一 Worker 发现并隔离处理两个 ACTIVE 租户 |
| `worker-allowlist` | 通过 | Worker Principal 可用后台端点，业务 CRUD 返回 403 |
| `gateway-real-request-quota` | 通过 | 配额依据真实 method/path/IP/body 执行，超限请求拒绝 |
| `webhook-ssrf` | 通过 | 私网与 metadata 目标在创建/投递边界被拒绝 |
| `url-deep-link-mobile-shell` | 通过 | 管理端深链和客户、伙伴、司机、RF 窄屏终端可直接打开 |

## 全量门禁

以下命令在同一被测 commit 上按顺序通过：

| 命令 | 结果 |
| --- | --- |
| `pnpm format:check` | 通过 |
| `pnpm db:migrate:deploy` | 通过；空库部署 82 个迁移 |
| `pnpm migration:safety` | 通过；本工作包无新增迁移 |
| `pnpm verify` | 通过；API 388、Web 37、Worker 31、Shared 31、UI 8 |
| `pnpm build` | 通过 |
| `pnpm test:e2e` | 12/12 通过 |
| `pnpm test:p3-e2e` | 3/3 通过 |
| `pnpm test:p4-e2e` | 2/2 通过 |
| `pnpm test:p5-readiness` | 5/5 通过；列表/命令/RF P95 为 88.66/40.81/27.01 ms，RPO 0、RTO 3.45 s |
| `pnpm test:v2-e2e` | 5/5 测试、11/11 场景通过 |
| `pnpm audit --prod --audit-level high` | 通过；0 个 High/Critical，4 个 Moderate |
| `git diff --check` | 通过 |
| `docker compose config` | 通过 |

CI 保留 verify/build、迁移安全、生产依赖审计、固定 Trivy 阈值和 P5 readiness；新增独立 V2 runtime job、同分支过时运行取消，以及失败时的 Playwright report、API/Worker/Web 与 migration 日志上传。

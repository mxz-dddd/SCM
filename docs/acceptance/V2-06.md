# V2-06 验收记录

- 新增平台公共 `NumberingFacade`，按租户与组织规则回退，以 PostgreSQL 原子更新分配单号；支持周期重置、模板日期令牌校验、宽度耗尽、审计、Outbox 和幂等重放。
- OMS、WMS、TMS、AMS、Billing 与 Control 的业务单据号统一调用编号门面；生产缺少规则时明确返回 `NUMBER_RULE_NOT_CONFIGURED`，不回退时间戳、UUID 片段或随机数。
- seed 为平台租户和演示租户建立所有已使用业务类型的默认日重置规则；静态测试校验源码生成方式与 seed 覆盖完整性。
- 新增并发分配、同键重放、组织优先/租户回退、缺失规则和宽度耗尽数据库测试；40 路竞争分配无重复。
- 确认业务号租户唯一约束，并补充库存效期告警 `(tenant_id, alert_no)` 唯一索引及迁移。

# 数据保留、隐私、容量与租户迁移

- 保留策略按业务单据、审计、轨迹、附件和遥测分别配置。归档只接收各领域提供的不可变候选快照，不跨域查询内部表；legal hold 优先于删除。
- PII 按字段分类，展示和导出由权限决定。主体访问、更正、删除请求先验证身份，再向所属领域发命令；交易、审计和法定义务数据保留并记录理由，不能物理抹除流水。
- 容量计划覆盖请求、存储、事件、报表、导出，记录租户用量、配额、冷热层和自动扩缩容阈值。用量流水不可修改。
- 试点到生产迁移先做配置、主数据、开放业务依赖预检。目标侧导入后必须比较数量、金额、状态；有任一差异则失败，不进入完成态。
